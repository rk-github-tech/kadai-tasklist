<h1>Log in</h1>

<%= form_with(url: login_path, scope: :session, local: true) do |f| %>

<%= f.label :email %>
<%= f.email_field :email %>

<%= f.label :password %>
<%= f.password_field :password %>

<%= f.submit 'Log in' %>
<% end %>

<p>新規登録はこちらから！ <%= link_to '新規登録', new_user_path %></p>
